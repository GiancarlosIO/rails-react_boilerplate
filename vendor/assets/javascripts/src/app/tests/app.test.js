import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';
import expect from 'expect';

describe('test-app', () => {
  it('test', () => {
    expect(9).toBeA('number');
  });
})
